# Orchestrator Circle Policy
# Dependabot version updates are explicitly disabled to prevent race conditions 
# and redundant operations with the canonical Renovate configuration.
version: 2
updates: []
