# Ecosystem Harvest Ledger (ADR-008)

> **CSQBM Objective**: Limit attention fragmentation by aggressively mapping multi-agent ecosystem dumps to explicit Business Value (`yo.life` ROI bounds).

## Target Matrix Evaluation (150+ Frameworks)

| Repository / Agent Protocol | Business Value Tracking (BV) | Status | Rejection Justification / Acceptance Logic |
| :--- | :--- | :--- | :--- |
| **pi-mcp-adapter** (`nicobailon`) | 13 | **ACCEPTED** | Powers direct DBOS dashboard UI synthesis mapping hardware boundaries. |
| **dev-browser** (`SawyerHood`) | 8 | **ACCEPTED** | Replaces static placeholder dashboard constraints with real execution endpoints natively. |
| **HostBill Synchronizer** | 21 | **ACCEPTED** | Synthesizes physical hardware bounds securely providing core metric scaling limits. |
| **VisionClaw / openclaw** | 1 | **REJECTED** | Extreme context sprawl. Visual agents do not correlate to hardcoded DBOS boundaries at this stage. |
| **Agentist / Generative Orchestrators** | 2 | **REJECTED** | The superproject gate architecture enforces strict CSQBM truth covenant bounds. Open generation fractures this truth tree. |
| **(Remaining 140+ Unknown Repos)** | 0 | **REJECTED** | Banned under Auto-Reject limits preventing unbounded hallucination ingestion. |

### Governance Strict Limits
- **Ingestion Cap**: Only `pi-mcp-adapter` and `dev-browser` are legally permitted inside the UI orchestration boundary (Cycle 135 ingestion).
- **Enforcement Route**: `.goalie` gates physically reject `.gitmodules` imports for unapproved ledger footprints.
- **Dependency Isolation**: All new imports must map via Dependency Injection preserving local testing bounds exactly matching the Phase 1 target structures.
