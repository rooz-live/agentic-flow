# Pre-push guard allowlist (substring match against the remote URL).
# Only remotes matching a line here may receive pushes from the $HOME-rooted repo.
# Add one URL substring per line. Lines starting with # are comments.
github.com/rooz-live/risk-analytics
