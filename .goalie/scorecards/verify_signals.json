{
  "_comment": "Signals run by scorecard_gate.py --verify to DERIVE coherence (override the self-asserted field). coherence=PASS iff all required signals exit 0. Tune freely.",
  "signals": [
    { "name": "cargo-check", "cmd": ["cargo", "check", "--quiet"], "required": true },
    {
      "name": "pytest",
      "cmd": ["python3", "-m", "pytest", "tests/", "--rootdir=tests", "-q", "--tb=line", "--ignore=tests/integrations"],
      "required": true
    },
    {
      "_comment": "placeholder for a capability-index / no-invented-paths probe; make required:true once implemented",
      "name": "no-invented-paths",
      "cmd": ["bash", "-c", "true"],
      "required": false
    }
  ]
}
