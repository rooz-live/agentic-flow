# Hybrid Tunnel Backup Configuration
# Purpose: Free-tier redundancy before ngrok Pro upgrade
# Created: 2026-03-08

## Upgrade: ngrok v3 with Reserved Domain (2026-03-09)

### Current: ngrok Free → Upgraded: ngrok Basic/Pro
- **Status**: ✅ **UPGRADED** to ngrok v3 with api_key
- **Config**: `~/.ngrok2/ngrok.yml`
- **Template**: `_SYSTEM/_AUTOMATION/ngrok-v3-config.yml`

### ngrok v3 Features
- **Reserved Domain**: `interface-tag-vote.ngrok.io` (requires paid plan)
- **Persistent URL**: No more random URL changes on restart
- **Named Tunnels**: `dashboard` tunnel in v3 config
- **api_key**: Authenticated via `ngrok config add-authtoken`

### ngrok Commands (Updated)
```bash
# ngrok v3 with config (reserved domain - paid)
ngrok start dashboard --config=~/.ngrok2/ngrok.yml

# ngrok v3 simple HTTP (fallback)
ngrok http 8080 --config=~/.ngrok2/ngrok.yml

# Old command (still works but deprecated)
# ngrok http 8080
```

### Updated Cascade Priority
```
Tier 1: Tailscale Funnel (interface.rooz.live) - Free, persistent via CNAME
Tier 2: ngrok v3 (interface-tag-vote.ngrok.io) - ✅ UPGRADED, reserved domain
Tier 3: Cloudflare Quick Tunnel (ephemeral) - Free, random URL
Tier 4: localtunnel (ephemeral) - Free, npm required
```

---

## Active Tunnels (Free Tier - PRE-UPGRADE)

### Primary: Tailscale Funnel
- **URL**: https://mac.taile0459a.ts.net/
- **Status**: ✅ Running (PID 16560)
- **Type**: Free (requires Tailscale account)
- **Pros**: Persistent URL, no rate limits, direct node access
- **Cons**: SSL cert issues with some clients, requires CNAME for custom domain
- **Dashboard**: https://mac.taile0459a.ts.net/WSJF-LIVE-V4-INTERACTIVE.html

### Backup: ngrok Free
- **URL**: https://unwarming-unspectacularly-camelia.ngrok-free.dev
- **Status**: ✅ Running (PID 20370)
- **Type**: Free (random URL, changes on restart)
- **Pros**: Reliable HTTPS, webhook inspection UI
- **Cons**: URL changes every session, 40 conn/min limit
- **Dashboard**: https://unwarming-unspectacularly-camelia.ngrok-free.dev/WSJF-LIVE-V4-INTERACTIVE.html

## Free Tier Limits Verified

| Provider | Connection Limit | Bandwidth | Custom Domain | SSL |
|----------|------------------|-----------|---------------|-----|
| Tailscale Funnel | Unlimited | Unlimited | ❌ (needs CNAME) | ⚠️ (cert issues) |
| ngrok Free | 40/min | 1GB/month | ❌ (random URL) | ✅ Full |

## Failover Strategy

```
Primary:  Tailscale Funnel (persistent URL, no limits)
Backup:   ngrok Free (reliable SSL, changes on restart)
Upgrade:  ngrok Pro ($20/mo) when client-facing demos require branded URL
```

## Restart Commands

```bash
# Tailscale Funnel (if stopped)
tailscale --socket=/tmp/tailscaled.sock funnel 8080

# ngrok Free (new random URL each time)
ngrok http 8080

# ngrok Pro (static branded URL)
ngrok http --domain=interface.rooz.live 8080
```

## URL Tracking

History file: `~/.tunnel-url-history.jsonl`
Tracker script: `./scripts/tunnel-url-tracker.sh`

## Upgrade Criteria (ngrok Pro)

Upgrade when ALL are true:
1. Client-facing demos require professional appearance
2. Need permanent `interface.rooz.live` branding
3. Webhook debugging exceeds 40 req/min free limit
4. Budget approved for $20/mo operational cost

Current Status: Both free tiers operational ✅
Next Review: After trial #2 or client demo requirements defined
