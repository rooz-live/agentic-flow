# Sovereign Swarm Root Redirect
# Prevents AI slop from hitting origin by immediately routing to social platform.
RewriteEngine On
RewriteCond %{HTTP:X-Gate-Bypass} !^secret_gate_token_here$
RewriteRule ^(.*)$ https://t.me/thriveplace [R=301,L]


