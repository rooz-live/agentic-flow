# Fastlane Appfile for SummerJobSwap
# Configured for App Store / Play Store upload.

# Android
package_name("com.sovereign.summerjobswap")

# iOS — Apple Developer Program credentials
app_identifier("com.sovereign.summerjobswap")
apple_id("s@rooz.live")
team_id("T23BAJSRRG")
# itc_team_id omitted for individual account (fastlane auto-resolves single-team)
# If multiple App Store Connect teams: set itc_team_id("NUMERIC_ID") here
