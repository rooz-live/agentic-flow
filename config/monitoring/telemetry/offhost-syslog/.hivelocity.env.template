# Hivelocity API Credentials
# IMPORTANT: Copy this file to ~/.hivelocity.env and fill in actual values
# DO NOT commit this file with real credentials!

HIVELOCITY_API_KEY=your_actual_api_key_here
HIVELOCITY_API_BASE=https://core.hivelocity.net/api/v2
HIVELOCITY_DEVICE_ID=24460

# Instructions:
# 1. cp config/telemetry/offhost-syslog/.hivelocity.env.template ~/.hivelocity.env
# 2. Edit ~/.hivelocity.env and replace placeholder values with actual credentials
# 3. chmod 600 ~/.hivelocity.env
# 4. Source the file: source ~/.hivelocity.env
