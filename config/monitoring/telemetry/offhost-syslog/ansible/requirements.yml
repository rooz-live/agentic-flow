# Ansible Requirements - Off-Host Syslog Black Box Recorder
# Required collections and roles

collections:
  - name: community.general
    version: ">=6.0.0"
  - name: ansible.posix
    version: ">=1.4.0"
  - name: community.crypto
    version: ">=2.0.0"

roles:
  # Security hardening role
  - name: dev-sec.os-hardening
    version: ">=5.0.0"
    src: https://github.com/dev-sec/ansible-os-hardening.git
