---
# =============================================================================
# rsyslog Role - Handlers
# =============================================================================

- name: restart rsyslog
  ansible.builtin.systemd:
    name: rsyslog
    state: restarted

- name: reload rsyslog
  ansible.builtin.systemd:
    name: rsyslog
    state: reloaded
