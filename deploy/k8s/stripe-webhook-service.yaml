apiVersion: v1
kind: Service
metadata:
  name: stripe-webhook
  namespace: billing-sovereign
  labels:
    app: stripe-webhook
spec:
  type: ClusterIP
  selector:
    app: stripe-webhook
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
