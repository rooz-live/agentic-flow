# macOS local variables
required_tools:
  - node
  - python3
  - ansible
  - terraform
  - kubectl
  - helm
  - semgrep
  - scorecard
  - jq
  - yq

brew_services_expected:
  - { name: grafana, state: started }
  - { name: postgresql@14, state: started }

launchd_agents_expected:
  - com.agentic-flow.ssl-monitor
  - com.bhopti.roam.staleness.watchdog
  - com.bhopti.swarm.supervisor
