# Agentic QE v3 Configuration
# Generated by aqe init
# 2026-04-01T02:24:24.332Z
#
# NOTE: Your customizations are PRESERVED when you run "aqe init" again.
# You do NOT need to re-run "aqe init" after editing this file - changes
# take effect immediately. The following settings are merged on reinstall:
#   - domains.enabled (custom domains like visual-accessibility)
#   - domains.disabled
#   - learning.enabled
#   - hooks.* preferences
#   - workers.enabled
#   - agents.maxConcurrent and defaultTimeout

version: "3.8.11"

project:
  name: "validators"
  root: "/Users/shahroozbhopti/Documents/code/investing/agentic-flow/scripts/validators"
  type: "single"

learning:
  enabled: true
  embeddingModel: "transformer"
  hnswConfig:
    M: 8
    efConstruction: 100
    efSearch: 50
  qualityThreshold: 0.7
  promotionThreshold: 3
  pretrainedPatterns: true

routing:
  mode: "hybrid"
  confidenceThreshold: 0.7
  feedbackEnabled: true

workers:
  enabled:
    - "pattern-consolidator"
    - "routing-accuracy-monitor"
  intervals:
    pattern-consolidator: 1800000
    coverage-gap-scanner: 3600000
    flaky-test-detector: 7200000
    routing-accuracy-monitor: 900000
  maxConcurrent: 2
  daemonAutoStart: true

hooks:
  claudeCode: true
  preCommit: false
  ciIntegration: false

skills:
  install: true
  installV2: true
  installV3: true
  overwrite: false

domains:
  enabled:
    - "test-generation"
    - "test-execution"
    - "coverage-analysis"
    - "quality-assessment"
    - "defect-intelligence"
    - "requirements-validation"
    - "code-intelligence"
    - "security-compliance"
    - "contract-testing"
    - "visual-accessibility"
    - "chaos-resilience"
    - "learning-optimization"
  disabled:

agents:
  maxConcurrent: 5
  defaultTimeout: 60000
